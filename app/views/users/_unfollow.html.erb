<% 
=begin 
%> Comment: original form w/o Ajax:
	<%= form_for(current_user.relationships.find_by_followed_id(@user), 
															html: { method: :delete }) do |f| %>
<% 
=end 
%>

<% 
=begin 
%> 
	Comment: Form with Ajax, notice the remote: true
<% 
=end 
%>
<%= form_for(current_user.relationships.find_by_followed_id(@user), 
														html: { method: :delete }, 
														remote: true) do |f| %>
  <%= f.submit "Unfollow", class: "btn btn-large" %>
<% end %>